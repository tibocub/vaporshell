# Bash compatibility coverage

What vaporshell's default (bash) mode does and does not do compared with bash
itself, measured rather than asserted, and how to keep this current when bash
releases a new version.

> Generated by `tests/coverage/gen-docs.py` from `tests/coverage/templates/`
> and `tests/coverage/probes.txt`. The tables are measurements; edit the
> template or the probes, not this file.

## Reference

<!-- REFERENCES -->

The target is **bash 5.3**. Earlier measurements in `docs/modes.md` were made
against bash 5.2.21; the differences found between the two are recorded in
the release log below.

<!-- SUMMARY -->

## How this is measured

`tests/coverage/probe.py` runs each probe (a few lines of shell, see
`probes.txt`) under the real bash, under dash, and under vaporshell in both
modes, and compares **stdout and exit status**. It does not compare stderr:
error wording is shell-specific.

- **ok**: vaporshell's output and exit status equal bash's.
- **differs**: they do not. For probes marked *bash ...* in the note this is a
  feature vaporshell does not implement yet; nothing here is a known bug
  (known bugs are fixed or listed under *Known deliberate differences*).
- 320+ probes is a sample of behaviour, not a proof of conformance. Areas
  without probes are listed under *Not covered*.

## Keeping up with bash releases

When bash publishes a new release:

1. Build it (`./configure --without-bash-malloc && make`) and note the exact
   version printed by `bash --version`.
2. Re-run the suites against it:
   `BASH_REF=/path/to/new/bash sh tests/run-suites.sh build/vaporshell`.
   A failure is either a bash behaviour change or a vaporshell bug; the
   suite name says which mode.
3. Regenerate this document:
   `python3 tests/coverage/gen-docs.py build/vaporshell --bash /path/to/new/bash`
   and `git diff docs/`. Rows that flipped between ok and differs are the
   observable changes.
4. Read the new section of bash's `NEWS` (it is in the source tarball) and add
   a row per relevant item to the release log below. `CHANGES` has the
   bug-fix level detail.
5. Record what was measured and against which build in the release log.

## Release log

| bash | how measured | observed change vs previous |
|---|---|---|
| 5.2.21 | Ubuntu package; used for the first `docs/modes.md` table | baseline |
| 5.3.0(1)-release | built from `bash-5.3.tar.gz`; probes + all suites | (1) `bash --posix` accepts function names that are not identifiers (`foo-bar()`); 5.2.21 rejected them. Matches NEWS item *tt*. (2) `$LINENO` inside a multi-line `eval "..."`: 5.2.21 counts from the line the eval command *ends* on (16, 17 in `tests/modes/bash/lineno.sh`), 5.3.0 from the line it *starts* on (15, 16); vaporshell follows 5.3.0. Found because the suite failed against 5.2.21; CHANGES for 5.3-alpha has a nearby entry about `LINENO` and subshells, but that it is this fix is not verified. No other change in the 12 mode-behaviour probes re-measured, and none in the other suites (all pass on both). |

### bash 5.3 NEWS, item by item

Status is against vaporshell's bash mode. "not implemented" means the feature
is absent; "n/a" means it belongs to something not built (readline, job
control, arrays, `[[`, `time`, coprocesses); "verified" means a probe or
test compares it with bash 5.3.

| NEWS | change | vaporshell |
|---|---|---|
| a | binary-file check looks at the first two lines when line 1 is `#!` | not implemented |
| b, f, o, u, aa, bb, cc, dd, jj | readline / programmable completion behaviour | n/a (line editor and completion not built) |
| c, ss | better error text for unterminated compound commands / EOF | not implemented (stderr wording is not compared) |
| d, ii, qq, rr, mm, nn, oo | job list and notification behaviour, `wait -n` | n/a (no job control); `wait` itself works |
| g, ee | `[[` regex errors; `test` parenthesised sub-expressions with >4 args | `[[` n/a; the `test` case checked once, same result |
| h | `umask` gained features for full POSIX conformance | symbolic modes and `-S` implemented and verified against dash and bash; the exact 5.3 additions were not itemised |
| i | `type -a -P` | not implemented (`type -t` is) |
| j | `trap -P` | not implemented |
| k | `command` before a declaration builtin keeps assignment parsing | n/a (`declare` not built) |
| l | `printf` `%q`/`%Q` alternate form | `%q` implemented and verified; `%Q` and the `#` form not implemented |
| m | `printf` `%ls`/`%lc` wide strings | not implemented |
| n | `patsub_replacement` default | n/a (`${x/pat/rep}` not built) |
| p | NULL `$PATH` equals `.` | not verified |
| p (2) | loadable builtins `kv`, `strptime` | n/a (no loadable builtins; the native plugin system is planned) |
| q | `GLOBSORT` | not implemented (assignment is accepted, has no effect) |
| r | `compgen -V` | n/a |
| s | `${ command; }` / `${|command;}` | not implemented |
| t | `array_expand_once` | n/a (arrays not built) |
| v | `TIMEFORMAT` precision | n/a (`time` keyword not built) |
| w | `BASH_MONOSECONDS` | not implemented |
| x | `BASH_TRAPSIG` | not implemented |
| y | `checkwinsize` in subshells | n/a |
| z | posix-mode `test` `<` `>` use the locale | not implemented (byte comparison) |
| ff | coprocs on by default | n/a (`coproc` not built) |
| gg | `bind -p NAME` | n/a |
| hh | `$BASH` when invoked as `su` | n/a |
| kk | `source -p PATH` | not implemented |
| pp | `bash_source_fullpath` | n/a (`BASH_SOURCE` not built) |
| tt | posix mode accepts non-identifier function names | **deliberately different**: vaporshell's POSIX mode follows dash, which rejects them (`tests/modes/posix/function_names_strict.sh`); bash mode accepts them (verified) |
| uu | `exit` in a trap uses `$?` from before the trap (POSIX interp 1602) | trivial case matches bash; the subshell distinction was not tested |
| vv | `fltexpr` loadable builtin | n/a |
| ww | build system | n/a |

## Coverage by area

Each table lists probes for one area; the count is probes matching bash.

<!-- PROBE-TABLES -->

## Builtins against bash's own list

Every builtin `compgen -b` reports in the reference bash, and whether
vaporshell (bash mode) has it as a builtin.

<!-- BUILTINS -->

## Known deliberate differences

- **`BASH_VERSION`** is `5.3.0(1)-vaporshell`. Scripts that parse the number
  work; scripts that require the `-release` suffix do not.
- **`RANDOM`** uses its own generator, so seeded sequences differ from bash's
  (the range, `0..32767`, and that seeding is repeatable, are the same).
- **`\u`/`\U` escapes** in `echo -e` and `printf` always produce UTF-8.
  bash's output there depends on the locale.
- **`declare -x`/`-r` listings** (`export`, `readonly` with no arguments) use
  bash's format and sort order.
- **`trap` with no arguments** still differs from bash in one
  environment-dependent way, measured against bash 5.3.0: bash also lists
  signals that were *ignored on entry* (`trap -- '' SIGINT` when the shell was
  started under `nohup` or as a background job). Tests that print a trap
  listing must therefore filter for the line they mean (`tests/own/traps.sh`
  and `tests/modes/bash/trap_pseudo.sh` do). Inside a subshell, `$(...)` or
  pipeline bash mode prints the parent's traps until the subshell sets one of
  its own, as bash does; POSIX mode prints nothing, as dash does.
- **Signals on NuttX** work (`trap`, `kill`, self-signals, ignoring, resetting,
  traps inside in-process subshells). Two NuttX facts limit what they can do,
  both measured in the simulator: without `CONFIG_SIG_DEFAULT` a signal nobody
  handles has no default action, so `kill` cannot terminate a task (not even
  with `SIGKILL`; with the option on, only the shell's own `kill -KILL $$`
  ended a task), and the console does not raise `SIGINT` unless
  `CONFIG_TTY_SIGINT` is set.
- **Expansion errors** are milder than bash's: a failing `${...}` (a bad
  substitution, `${x:1:-9}`, `${!unset}`, `failglob`) fails that command and
  the shell goes on, where bash aborts the rest of a `-c` command list and, for
  a *bad substitution*, exits a script. `${@/pat/rep}` and other per-element
  operators on `$@` are rejected rather than applied to each parameter.
- **Collation**: glob results and `[[ a < b ]]` are ordered by the collation
  locale (`LC_ALL`, then `LC_COLLATE`, then `LANG`, following assignments) using
  `strcoll`, as bash does, so `a.txt` sorts before `B.txt` under en_US.UTF-8.
  A locale that is not installed leaves the ordering as it was. `test`'s `<` and
  `>` stay byte order, as in bash outside posix mode. NuttX has no locales and
  always sorts bytes; the NuttX differential runner therefore runs its reference
  shells with `LC_ALL=C`.
- **`shopt`** knows bash 5.3's 59 options with their defaults and formats. Nine
  change behaviour (`nullglob`, `dotglob`, `failglob`, `nocaseglob`,
  `nocasematch`, `extglob`, `globstar`, `expand_aliases`, `patsub_replacement`),
  `inherit_errexit` and `xpg_echo` switch existing mode features, interactive
  ones (`histappend`, `checkwinsize`, ...) are accepted and remembered but do
  nothing, and the rest (`lastpipe`, `compat31`..`compat44`, `cdable_vars`,
  `extdebug`, `localvar_inherit`, `localvar_unset`, ...) are an error to *set*
  so a script that depends on them fails loudly instead of quietly running
  wrong.
- **`[[ ]]`**: `=~` uses POSIX ERE from `regcomp` (host only for now) and does
  not set `BASH_REMATCH`, which needs arrays. The operands of `-eq` etc. are
  arithmetic expressions and extglob patterns are always recognised on the
  right of `==`, both as in bash.
- **`DEBUG` trap**: runs before simple commands, `for` iterations, `case`,
  `[[` and `((` at the top level; not inside functions (as bash without
  `set -T`), and not once per pipeline stage in the parent as bash does.
- **`time`** reports user and system time as zero on NuttX (only wall time is
  available there).
- **`ulimit -a`** and **`hash -l`/`-t`** are not implemented; `hash` lists
  entries newest-first where bash's order is its hash order.
- **`kill -l`** and **`set -o`** list only the signals and options vaporshell
  has, not bash's full sets.

## Not covered

No probes exist for: interactive behaviour (line editing, history,
completion, prompts, job control), `PS1`-`PS4` expansion beyond `PS4` under
`set -x`, locale-dependent behaviour, `set -x` trace formatting, `mapfile`,
`coproc`, the `shopt` options that only matter interactively, and error-message text. "ok" above says nothing
about these.
